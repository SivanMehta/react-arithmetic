import Operand from './operand';

export class One extends Operand {
  value() {
    return 1;
  }
}

export class Two extends Operand {
  value() {
    return 2;
  }
}

export class Three extends Operand {
  value() {
    return 3;
  }
}
